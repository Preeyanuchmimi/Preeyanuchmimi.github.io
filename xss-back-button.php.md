# รายงานการแก้ไขช่องโหว่ Cross-Site Scripting (XSS) แบบ Reflected
---
หน้าเว็บที่เลือกมาวิเคราะห์มี Business Function หลักคือการทำ DNS Lookup โดยให้ผู้ใช้กรอก Domain Name แล้วระบบจะแปลงและแสดงผลเป็น IP Address ที่เกี่ยวข้อง
กระบวนการทำงานคือ ผู้ใช้กรอกโดเมน → ระบบประมวลผลค้นหา DNS → แสดง IP Address พร้อมปุ่ม “Go Back” สำหรับย้อนกลับไปหน้าก่อนหน้า
ช่องโหว่ที่พบไม่ได้เกิดจากฟังก์ชันหลักในการแปลง Domain เป็น IP แต่เกิดจากส่วนแสดงผลของปุ่มย้อนกลับที่นำค่าจาก $_SERVER['HTTP_REFERER']
มาใช้งานโดยไม่เข้ารหัส ทำให้เสี่ยงต่อการโจมตีแบบ Reflected XSS.
ตัวอย่าง Business Function จุดที่เกิดช่องโหว่



![cf](BB.png)



![cf](3.1.jpg)


---



# สาเหตุของปัญหา:

มีช่องโหว่ประเภท Reflected Cross-Site Scripting (XSS) อยู่ที่ส่วนปุ่ม Go Back เนื่องจากมีการนำค่าจาก $_SERVER['HTTP_REFERER']
ซึ่งเป็นข้อมูลที่ผู้ใช้ควบคุมได้ มาแสดงผลโดยไม่เข้ารหัส ทำให้ผู้โจมตีสามารถฝังโค้ด JavaScript ลงไปได้

![cf](1.1.png)

วิธีทดสอบ ลองแก้ไขค่า Referer (เช่น ผ่านเครื่องมือ Proxy หรือปลั๊กอินแก้ Header) แล้วใส่โค้ดทดสอบ เช่น
<script>alert('XSS')</script> หากหน้าเว็บแสดงผลหรือรันสคริปต์ดังกล่าว แสดงว่ามีช่องโหว่ Reflected XSS จริง
หรือสามารถยืนยันได้จากผลการสแกนด้วยเครื่องมือด้านความปลอดภัยที่แจ้งเตือนตำแหน่งโค้ดที่มีความเสี่ยง.
จากการทดสอบด้วยเครื่องมือสแกนช่องโหว่ เช่น RIPS พบว่าระบบมีช่องโหว่ประเภท Reflected XSS จริง โดยเครื่องมือระบุจุดที่มีการนำค่าจาก $_SERVER['HTTP_REFERER'] มาแสดงผลโดยไม่ผ่านการเข้ารหัส ทำให้มีความเสี่ยงต่อการฝังและรัน JavaScript

---
# ปัญหาอยู่ที่ไฟล์
xss_back_button.php


# วิธีแก้ที่ถูกต้อง

## ตรวจสอบปัญหาจากผลการสแกน (RIPS)

ขั้นตอนแรกให้กดเครื่องหมายปุ่มที่บอกรายละเอียดปัญหา ว่าต้องแก้อย่างไรกับปัญหาที่พบเจอจากเครื่องมือสแกน

![cf](1.3.png)

---

## เชื่อมต่อเข้าสู่เซิร์ฟเวอร์ด้วย WinSCP
เปิดโปรแกรม **WinSCP** เชื่อมต่อเข้าสู่เซิร์ฟเวอร์ เข้าไปยังโฟลเดอร์ของเว็บไซต์  
ตรวจสอบไฟล์ที่ถูกแจ้งเตือนจาก RIPS  
พบว่าไฟล์ที่เกี่ยวข้องคือ  xss_back_button.php บรรทัดที่ถูกแจ้งเตือนไม่ได้ถูก Comment ไว้  
แสดงว่าเป็นโค้ดที่ยังทำงานอยู่จริง
![cf](1.11.png)
 
---

## เปิดไฟล์ด้วย Visual Studio Code
เปิดไฟล์ `xss_back_button.php` ตรวจสอบตำแหน่งที่เกิดช่องโหว่วิเคราะห์การทำงานของโค้ด  

![cf](1.2.png)

---

# แก้ไขจุดปัญหาที่พบเจอ
แนวทางการแก้ไขคือ ไม่ควรนำค่าจาก $_SERVER['HTTP_REFERER'] มาแสดงผลโดยตรง เนื่องจากเป็นข้อมูลที่ผู้ใช้สามารถปลอมแปลงได้และอาจถูกใช้ฝังสคริปต์อันตรายได้ วิธีแก้ไขเบื้องต้นคือการทำ Output Encoding ก่อนแสดงผล โดยใช้ฟังก์ชัน htmlspecialchars() เพื่อเข้ารหัสอักขระพิเศษ ป้องกันการรัน JavaScript บนหน้าเว็บ
## 🔴 ก่อนแก้ไข

![cf](1.2.png)

---

## 🟢 หลังแก้ไข

วิธีแก้คือเช็ค document.referrer แทน
document.referrer → จะมีค่าก็ต่อเมื่อผู้ใช้มาจากหน้าอื่นจริงๆ (คลิก link มา)
ถ้าเปิดตรงๆ หรือ referrer ว่าง → ไป portal.php แทน

![cf](1.6.png)

---

# การอัปโหลดไฟล์ที่แก้ไขแล้ว (แทนที่ไฟล์)

หลังจากแก้ไขปัญหาที่พบเจอแล้ว ให้เซฟและนำไฟล์ที่ถูกต้องไปวางแทนที่ไฟล์เดิมที่อยู่ในเซิร์ฟเวอร์  (ก่อนจะลบไฟล์เดิมต้องมีการ Blackup ข้อมูลจากไฟล์เดิมเก็บไว้ในอีกโฟล์เดอร์)


![cf](1.7.png)

## อัปโหลดไฟล์ใหม่ที่แก้ไขแล้วแทนที่

![cf](1.8.png)

---


#  สำเร็จ!!
เมื่อแก้ไขปัญหาเสร็จแล้ว เช็ค RIPS อีกครั้ง จากเป็นหน้าตอนแรกเป็น 2 เมื่อแก้เสร็จแล้วจะขึ้นเหลือ 1


![cf](1.12.png)


# ตรวจสอบ
ก่อนแก้ไข

- ไม่สามารถกดปุ่ม **Go Back** ได้  

![cf](1.9.png)

หลังแก้ไข

- สามารถกดปุ่ม **Go Back**  
- ย้อนกลับไปหน้า `commandi.php` ได้ตามปกติ  

![cf](LL.png)

---

# ผลลัพธ์หลังการแก้ไข
หลังจากทำการแก้ไขโค้ดเพื่อปิดช่องโหว่ Reflected XSS แล้ว ได้ทำการทดสอบการทำงานของระบบอีกครั้ง โดยทดลองกรอก Domain Name เพื่อให้ระบบทำการ DNS Lookup ผลการทดสอบพบว่า ระบบยังสามารถแปลง Domain เป็น IP Address ได้ตามปกติ และปุ่ม “Go Back” ก็สามารถใช้งานได้ตามเดิม
แสดงให้เห็นว่าการแก้ไขโดยการทำ Output Encoding หรือการปรับลิงก์ให้ปลอดภัยนั้น ไม่ได้กระทบต่อ Business Function หลักของระบบ ซึ่งก็คือการทำ DNS Lookup ดังนั้น แนวทางการแก้ไขนี้ถือว่าเหมาะสม เพราะสามารถปิดช่องโหว่ด้านความปลอดภัยได้ โดยไม่ส่งผลกระทบต่อการทำงานเดิมของระบบ.
